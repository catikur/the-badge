# Golden Set Deposu
Format: JSONL — her satır { id, input, expect }. `expect` üretken çıktı için
KALİTE RUBRİĞİDİR: must_include (geçmesi zorunlu ögeler), yasak (asla olmamalı),
ton, max_cumle. Hedef: özellik başına 20-50 örnek (FAZ 04'te tamamlanır).
Eval koşusu: script + insan bakışı karışımı; eşik altı = merge yok (CLAUDE.md).
